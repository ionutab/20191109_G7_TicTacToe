public class Move {

    int line;
    int col;

    /*

        TODO: use this in TicTacToe - readMove as return type
        then validateMove(Move move)
        makeMove(Move move)
        isWin(Move move)
     */
    public Move(int line, int col) {
        this.line = line;
        this.col = col;
    }
}
